#include <bits/stdc++.h>
using namespace std;
int main()
{
   float x,y,z, formula;
   cin >> x >> y >> z;
   formula=(7 + pow(2*x + y - z,2*x + y - z))/(2*x+y-z);
    cout << formula;
    return 0;
}
